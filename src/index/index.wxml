<view class="badge">
  <slot></slot>
  <view wx:if="{{dot}}" class="badge-dot my-badge"></view>
  <view wx:elif="{{count}}" class="badge-text my-badge">{{count>over?over+'+':count}}</view>
</view>